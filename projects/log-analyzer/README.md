# Log Analyzer

第一个贯穿项目：从单行日志解析开始，逐步演化为带统计、过滤、错误处理和 CLI 接口的日志分析工具。

## 当前任务

实现 `log_analyzer.py` 中的 `parse_log_line`，使当前测试通过。

## 运行测试

```powershell
python -m unittest discover -s tests -v
```

当前测试有意处于 RED 状态。完成第一课任务后应变为 GREEN。

